const fs = require("fs"); let code = fs.readFileSync("app/teacher/enter-scores/page.tsx", "utf8"); code = code.replace(".from(\"assessments\")\\n      .select(\"*\")", ".from(\"assessments\")\\n      .select(\"id, title, type, max_score, created_by, created_at, date\")"); code = code.replace(".from(\"scores\")\\n                .select(\"*\")", ".from(\"scores\")\\n                .select(\"id, exam_score, class_score, student_id, term_id, subject_id, total, grade, remarks\")"); fs.writeFileSync("app/teacher/enter-scores/page.tsx", code); console.log("Fixed overfetching in enter-scores");
